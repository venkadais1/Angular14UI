import { SalIndicatorPipePipe } from './sal-indicator-pipe.pipe';

describe('SalIndicatorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SalIndicatorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
